run:
  timeout: 5m
  modules-download-mode: readonly
  skip-dirs:
    - internal/ns
    - internal/skia
    - internal/w32
output:
  sort-results: true
linters-settings:
  depguard:
    list-type: blacklist
    include-go-root: true
    packages:
      - log
      - github.com/satori/go.uuid
  errcheck:
    check-type-assertions: true
    check-blank: true
  gocritic:
    enabled-tags:
      - diagnostic
      - performance
      - style
      - opinionated
      - experimental
    disabled-checks:
      - sloppyReassign
  gofumpt:
    extra-rules: true
  govet:
    check-shadowing: true
    enable-all: true
    settings:
      printf:
        funcs:
          - Errorf
          - Warnf
          - Infof
          - Debugf
          - Fatalf
          - StartRootSpanf
          - StartSubSpanf
          - EndWithMsgf
          - Newf
          - NewWithCausef
  misspell:
    locale: US
  staticcheck:
    go: "1.19"
    checks: [ "all", "-SA3000" ]
  unused:
    check-exported: false
linters:
  disable-all: true
  enable:
    - bodyclose
    - depguard
    - errcheck
    - errorlint
    - exportloopref
    - goconst
#    - gocritic
    - gofumpt
#    - goimports
    - gosec
    - gosimple
    - govet
    - ineffassign
    - misspell
    - noctx
    - prealloc
    - predeclared
    - revive
    - staticcheck
    - unconvert
    - unused
#    - wastedassign
# Once Go generics support is finalized in golangci-lint, re-enable the above linters
issues:
  max-same-issues: 10
  exclude-use-default: false
  exclude:
    - "fieldalignment: struct with \\d+ pointer bytes could be"
    - "G204:"
    - "G304:"
    - "package-comments: should have a package comment"
